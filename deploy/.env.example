# CivicAI production .env template
# Copy to .env in deploy/ and fill real values before `docker compose up`.

# PostgreSQL
DB_PASSWORD=change-me-strong-db-password

# JWT signing secret (>= 32 bytes). Generate with:
#   openssl rand -base64 48
JWT_SECRET=change-me-xxxxxxxxxxxxxx-change-me